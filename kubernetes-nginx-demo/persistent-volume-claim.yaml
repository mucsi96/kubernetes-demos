apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nginx-demo-pvc
  namespace: nginx-demo
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: manual
  resources:
    requests:
      storage: 10Mi
