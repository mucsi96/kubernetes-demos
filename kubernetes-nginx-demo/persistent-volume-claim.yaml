apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nginx-demo-pvc
  namespace: nginx-demo
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Mi
